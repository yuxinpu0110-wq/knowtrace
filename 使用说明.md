# 知溯 KnowTrace · 使用说明

面向演示者与验收人。README 讲「这是什么」，本文讲「怎么跑、怎么演、出问题怎么办」。

---

## 一、启动

### 方式 A：双击 HTML（零依赖，最稳）

**直接双击根目录的 `知溯KnowTrace.html`**，浏览器打开即进入登录页。

不需要装 Node.js、不需要联网、不需要 API Key，也不会被 Windows 智能应用控制拦截。
这是「评委 clone 下来直接运行」的主路径 —— 完整后端不参与，全链路走内置 Mock。

> 想接真实 DeepSeek、多账号进度落盘时，用下面的方式 B。

### 方式 B：单进程后端（需 Node.js 18+）

**Windows**：双击 `启动.bat`（非中文环境下若双击异常，改用 `start.bat`）
**macOS / Linux**：终端执行 `./启动.sh`

会出现一个控制台窗口并保持打开（关掉它服务就停了）。看到这一行即启动成功：

```
[backend] 服务已启动：http://localhost:3001（MOCK_DELAY_MS=250）
```

浏览器会自动打开。**若 3001 被占用**，控制台会打印：

```
[backend] 注意：3001 端口被占用，已自动改用 3002，请用上面的地址访问。
```

这时请手动访问 `http://localhost:3002/`（自动打开的也是这个地址）。

> **不要关掉那个控制台窗口** —— 它就是服务器。演示期间保持开启。

---

## 二、演示账号

| 账号 | 密码 | 显示名 |
|---|---|---|
| `admin` | `admin` | 超级管理员 |
| `Yuxinpu0110` | `abcde0110` | Yuxinpu |
| `Xgyr0613` | `tsy0821` | Xgyr |
| `Tujuanjuan` | `Lyx0623` | Tujuanjuan |
| `WenZhaobo0220` | `303603` | WenZhaobo |
| `liuhan666` | `liuhan666` | liuhan |
| `An1031` | `311007` | An |

演示建议用 `admin / admin`。账号数据在**方式 A**（双击 HTML）下存在浏览器 localStorage，在**方式 B**（后端）下存在 `backend/data/store.json`（删掉该文件重启即可重置）。

> **预置演示（方式 B 后端）**：`Yuxinpu0110`（1 个知识库：微积分）与 `Xgyr0613`（3 个知识库：军事理论、微积分、线性代数）
> 首次登录即带**已建好的图谱**，每个图谱随机点亮十几个节点（绿=已掌握、橙=薄弱），评委开箱即可看效果，
> 无需现场粘贴教材建图。用户自己建过图后不会被覆盖。

---

## 三、完整演示路径（约 5 分钟）

### 1. 登录
打开首页会跳转到 `/login`。输入 `admin` / `admin` → 进入首页。

**预期**：首页显示知识图谱。`Yuxinpu0110` / `Xgyr0613` 首次登录即带预置图谱；其它账号（如 `admin`）首次登录时图谱为空，需先导入教材建图。

### 2. 建图（`编辑` 按钮 → `/edit`）

把 `docs/线性代数知识集合.md` 全文粘贴进教材框，点「生成图谱」。

**预期**：约 1 秒内出图，提示 **139 个知识点**。这是纯前端 JS 计算，不再依赖 Python。

> 现场想更快：点「载入示例教材」用 `samples/textbook.md`（小教材，秒级）。

### 3. 提问（`提问` 按钮 → `/problem`）

输入：

> 用高斯消元法解含参数的线性方程组时，为什么会出现无穷多组解？

**预期**：系统判断题目在教材覆盖范围内，定位到 3 个相关节点（线性方程 / 高斯消元法 / 线性方程组），并给出一段基于教材的直接解答。

### 4. 诊断（提交后自动进入 `/test`）

按提示做 2~5 道选择题。**故意选错几道**，让系统收敛到薄弱点。

**预期**：图谱上橙色节点逐轮变亮，最终定位到一个具体薄弱节点（线性代数场景下为「主元与自由变量」）。

### 5. 学习（诊断完成后点节点 → `/learn`）

六步教学：概念 → 直觉 → 联系 → 示例 → 易错点 → 检测。

**预期**：每一步都有实质内容（内置场景下为预置内容，接了 DeepSeek 则为实时生成）。
点「没看懂」可以让系统换一种说法（最多 3 次）。

### 6. 验证

在最后一步选择正确答案并提交。

**预期**：通过后提示「你已掌握」，返回首页该节点**变为绿色**。

---

## 四、三个内置演示场景

`docs/` 下三份教材各自对应一个内置场景。**只要用对应教材建图，整个链路不依赖 API Key 即可完整跑通。**

| 教材 | 知识点数 | 场景聚焦的节点 |
|---|---|---|
| `docs/线性代数知识集合.md` | 139 | 主元与自由变量 / 高斯消元法 / 行阶梯形与行简化阶梯形 |
| `docs/微积分知识集合.md` | 188 | 连续的定义 / 函数的连续性 / 函数极限的定义 |
| `docs/军事理论知识集合.md` | 134 | 信息化战争的制胜要素 / 信息化战争的基本特征 |

**推荐提问**（复制粘贴即可）：

- 线性代数：`用高斯消元法解含参数的线性方程组时，为什么会出现无穷多组解？`
- 微积分：`如何用连续的定义说明函数 $\sin x$ 在 $x=0$ 处连续？`
- 军事理论：`信息化战争的制胜要素包括什么？`

> **提问技巧**：覆盖范围判断（这道题能否用当前教材回答）已交给 DeepSeek 判定——
> 后端模式下，简短问法如「什么是主元？」也能正确命中「主元与自由变量」，同时仍会拦截
> 「今天天气怎么样？」这类与教材完全无关的输入。离线双击 HTML（方式 A）没有 DeepSeek，
> 仍是关键词匹配，建议把知识点名称写全（如「主元与自由变量是怎么确定的？」）。

---

## 五、有 Key / 无 Key 的行为差异

**方式 B（后端）不内置任何 Key**：接真实 DeepSeek 需自填（左下角 ⚙️ 界面输入，或 `.env` 设 `DEEPSEEK_API_KEY`）。
真实模块优先、失败（断网 / Key 失效）自动回退 Mock，主链路不会崩。

**方式 A（双击 HTML）** 是纯离线演示，永远走内置 Mock，不接 DeepSeek。

界面上每个环节的结果都带一个来源标记：

| 标记 | 含义 |
|---|---|
| `mock: false` | 真实模块产出（接了 DeepSeek，或建图这种本就纯本地的计算） |
| `mock: true` | 回退到内置 Mock |

**想换成自己的 Key 或清空**：启动后点左下角 ⚙️ 填入 Key —— 界面输入的 Key 优先级最高，只存后端内存、
不写文件、不回传浏览器，**重启后端即失效**；或复制 `.env.example` 为 `.env` 填入 `DEEPSEEK_API_KEY`
（`.env` 已被 gitignore，不会入库）。优先级：界面设置 > 环境变量。

建图环节不受 Key 影响，永远走本地规则（这也是它稳定的原因）。

---

## 六、现场故障处理

| 现象 | 原因与处理 |
|---|---|
| 双击 `知溯KnowTrace.html` 白屏 | 换个浏览器（建议 Chrome / Edge 最新版）。文件 2.1MB 含内联字体，首次打开稍慢是正常的。 |
| 双击 `启动.bat` 窗口一闪而过 | 说明后端启动失败。改用 `start.bat`，或在该目录开终端执行 `node backend\server.js` 看报错。 |
| 提示「没有找到 Node.js」 | 仅**方式 B**需要。装 Node.js 18+：<https://nodejs.org/>，装完重新双击；或直接用**方式 A** 双击 HTML，不需要 Node。 |
| 提示「缺少前端构建产物」 | `frontend/dist/index.html` 不在。若是从源码跑来，先 `cd frontend && npm install && npm run build`。 |
| 浏览器打不开 / 地址栏 404 | 看控制台打印的**实际端口**（可能已自动改成 3002）。 |
| 页面白屏（后端模式） | 按 F12 看控制台报错；确认是用控制台里打印的地址访问的。 |
| 登录后刷新掉线 | 浏览器禁用了 Cookie。换浏览器或放开限制。 |
| 端口冲突反复出现 | 指定固定端口：`set PORT=8080` 后再启动，或用 `.env`。 |
| 想清空所有进度重来 | **方式 B**：删掉 `backend/data/store.json` 重启。**方式 A**：清除浏览器站点数据（localStorage）。 |

**答辩现场建议**：提前 10 分钟启动一次、走一遍完整链路，把教材提前粘好放在剪贴板，
避免现场打字。**不依赖网络**是这个交付包最大的优势，请善用。

---

## 七、公网演示（可选）

用赛事方的隧道能力把本机服务暴露成一个临时公网地址时，注意：

- 服务**保持运行**，控制台窗口不能关。隧道指向的是本机端口。
- 隧道地址是**临时的**，会随开发者空间的生命周期失效（体验版 3 小时；持久版断连超 12 小时销毁）。
- 因此**不要把它当作作品唯一的体验入口** —— 正式交付以代码仓库 + 本交付包为准。

服务默认监听全部网卡，隧道客户端在同机运行即可访问，无需额外配置。
若只想本机可访问，可在 `.env` 里设 `HOST=127.0.0.1`。

---

## 八、验证包是否完好（可选）

```bash
# 接口冒烟（服务需已启动）
node backend/scripts/smoke.mjs

# 建图移植的等价性对拍（需要本机有 Python，仅用于验证）
node backend/scripts/parity-md2graph.mjs
```

冒烟测试里 `question/locate` 与 `diagnosis/next` 两项在**微型测试图谱**下会显示 FAIL ——
这是测试断言过严（它断言定位必须有结果，而 Mock 定位器的设计原则是「不把超纲问题硬塞给固定节点」），
**开发版同样如此，与本次封装无关**。用真实教材（如线性代数 139 节点）时定位正常。
